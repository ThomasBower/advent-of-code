'use strict'

const parse = input => input.split('\n').map(line =>
  line.replace(')', '').split(' (contains ').map(s => s.split(/[ ,]+/g)));

const part1 = input => {
  return input
}

// Part 2
// ======

const part2 = input => {
  return input
}

module.exports = { part1, part2 }
